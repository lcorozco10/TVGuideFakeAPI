{
  "name": "fakeserver",
  "version": "1.0.0",
  "description": "fakeserverapp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server -p 3007 -w db.json"
  },
  "author": "lcorozco10",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.1"
  }
}
